import React from "react";
import ReactDOM from "react-dom/client";
import CryptoTerminal from "./CryptoTerminal.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CryptoTerminal />
  </React.StrictMode>
);
